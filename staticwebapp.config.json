{
    "routes": [
      {
        "route": "/feed/:slug",
        "rewrite": "/feed/:slug"
      },
      {
        "route": "/profile/:slug",
        "rewrite": "/profile/:slug"
      },
      {
        "route": "/group/:slug",
        "rewrite": "/group/:slug"
      },
      {
        "route": "/*",
        "rewrite": "/"
      }
    ]
  }
  